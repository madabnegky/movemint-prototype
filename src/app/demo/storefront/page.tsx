"use client";

import { DemoStorefront } from "@/components/demo/DemoStorefront";

/** The SSO path: the member arrives already authenticated from home banking. */
export default function DemoStorefrontPage() {
    return <DemoStorefront />;
}
